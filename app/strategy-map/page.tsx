import Link from 'next/link';
import { chapters, concepts } from '@/lib/data';
import SectionHeader from '@/components/ui/SectionHeader';
export default function StrategyMapPage() { return <div className="space-y-8"><SectionHeader eyebrow="战略地图" title="从十三篇到训练路径" description="把章节、概念、案例、训练连接成完整学习路径。" /><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{chapters.map((chapter) => <Link key={chapter.id} href={`/chapters/${chapter.slug}`} className="card block p-5"><div className="text-sm text-bronze">{chapter.order}. {chapter.theme}</div><h3 className="mt-2 font-serifcn text-xl font-bold text-paper">{chapter.title}</h3><p className="mt-2 text-sm text-paper/60">{chapter.structure.join(' → ')}</p></Link>)}</div><SectionHeader title="概念关系" /><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-4">{concepts.map((concept) => <Link key={concept.id} href={`/concepts/${concept.slug}`} className="soft-card block p-4"><div className="font-semibold text-paper">{concept.name}</div><p className="mt-1 text-sm text-paper/55">{concept.oneLineExplanation}</p></Link>)}</div></div>; }
